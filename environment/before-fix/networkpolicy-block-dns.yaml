apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dns-ingress
  namespace: kube-system
spec:
  podSelector:
    matchLabels:
      k8s-app: kube-dns   # THIS TARGETS COREDNS PODS
  policyTypes:
    - Ingress
  ingress: []  # 🚫 NO INGRESS ALLOWED TO COREDNS PODS
